---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: ubuntu2404
    image: ubuntu:24.04
    prebuild_image: true
    # privileged: true
    # command: /sbin/init
    # tty: true

provisioner:
  name: ansible
  log: true
  env:
    ANSIBLE_ROLES_PATH: ../../../../roles/

verifier:
  name: ansible

